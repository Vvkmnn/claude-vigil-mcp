# TODO: claude-vigil-mcp

## Code Quality

- [ ] **Add explicit return type** — 1 function missing return type in `snapshot.ts:570`. Low priority.

- [ ] **Re-enable `noUncheckedIndexedAccess`** — Currently disabled preemptively. Should be tested and re-enabled if the codebase handles it cleanly.

## Architecture

- [ ] **Add vigil methodology diagram** — The README `## methodology` section would benefit from an ASCII architecture diagram showing the snapshot/diff pipeline. User mentioned adding this separately.
